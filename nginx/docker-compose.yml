version: "3.7"

services:
  nginx:
    image: nginx
    ports:
      - "80:80"
      - "443:443"
    networks:
      - numeru-network
    volumes:
      - type: bind
        source: ./conf.d
        target: /etc/nginx/conf.d

networks:
  numeru-network: